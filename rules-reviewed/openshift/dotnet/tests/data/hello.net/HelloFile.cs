using System;

namespace HelloFile
{
    class Program
    {
        static void Main(string[] args)
        {
			FileStream fs = new FileStream(File.Open("World");
			FileSystemWatcher.watch(fs);
			
        }
    }
}